server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:bytechat}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:root}
  flyway:
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: pangasyamsundar1805@gmail.com
    password: jprorjnnluawjceo
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration: 86400000 # 24 hours
  refresh-expiration: 604800000 # 7 days

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
  api-docs:
    path: /v3/api-docs

cloudinary:
  cloud-name: "${CLOUDINARY_CLOUD_NAME:dxkoeutlb}"
  api-key: "${CLOUDINARY_API_KEY:678825641375837}"
  api-secret: "${CLOUDINARY_API_SECRET:uS9TJsgwuD4uL0TVZengOAuNW8s}"
